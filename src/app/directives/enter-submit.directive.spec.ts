import { EnterSubmitDirective } from './enter-submit.directive';

describe('EnterSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
